# 省心购科技
以下三个方向，你可以任选一个方向来聊（都想聊也行）
a) 整体agent框架，任意选一个源码，openclaw/openhands(重点考察框架设计，openclaw代码写的很烂）
b) memory，任意选一个源码，mem0/Acontext/Graphiti/MineContext（重点会考察如何结合业务设计）
c) ReAct框架设计，全部要看, openclaw/AutoGPT/Langgraph/openhands（重点考察各个框架的ReAct差异）最难
更多的，关于skills，关于tools，n8n/dify的差异，有兴趣也可以聊

先看这个源码的核心逻辑，然后基于核心逻辑再去展开细节

也可以聊聊你的

关于项目:
1. Nanobot 消息是怎么进来的, 通过 client 吗, 为什么这么设计
2. cron 和 heartbeat 怎么改造的, 放置的位置, 在主规划还是怎么样
3. 上下文是存在内存中吗, 怎么存的?
关于业务:
4. 护肤 agent 会选择什么维度作为评判是否存入 agent 的 Memory 中?:
	1. 跨会话的内容
	2. 联系性
5. 如果用户提出要你的回答简短一点, 你会把这一部分放入user prompt 还是 memory 部分?(放在 user prompt 部分, 因为如果下一次要让他调研, 那么需要长一点的生成, 肯定不是简短的)
6. 长短期记忆的区分怎么做, 他的边界在哪里, 有没有看 mem0 的论文, 短期记忆怎么转化为长期记忆
7. 一个新消息要进入 memory 的流程是怎么样的
8. memory 除了使用大模型来进行提取 fact, 还有没有别的方法
9. 如果使用大模型, 那么在高并发的情况下可能会延迟比较高, 你有什么优化方案(先进行一次规则匹配, 挡住一些噪音, 在使用小模型, 小模型也不行就切大模型)
10. memory 的写入链路中, 在实际业务上, 你最担心出现什么问题?(时间, 不能延迟太高, 否则用户感受差, 怎么处理, 放在后台异步)
快问快答:
11. 如果用户觉得 memory 检索太慢了, 你会优先怀疑哪几个环节(可能是 query 出现问题,过滤出现问题, 也有可能是召回出现问题)
12. mem0 使用了哪几种 vectordb? 默认的是哪个 db?
vibe-coding:
13. 实现一个你觉得优秀的 ReAct 框架, 包含两个 tool 的伪代码, 天气查询+计算器


# 字节跳动
