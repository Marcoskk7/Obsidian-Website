
拓展问答:
1. 你的 functioncall 怎么保证准确率?
答: 做了动态路由+prompt 调优(给出正反例), badcase 收敛入memory, 失败重试和参数校验, 以及我们提供了规范化的字段来给模型进行 observation，让它更好地 thinking 下一步。在 SimpleClaw 中, 实际用到的手段有:
- 缩小候选集合  
    通过子 Agent、stage、device capability 做硬隔离。
- 注入实时状态  
    每轮把图片状态、运行任务、用户档案、未完成 obligation 等重新加入上下文，避免模型根据旧对话误调用工具。
- 工具执行前硬校验  
    即使模型选中了工具，生命周期 Gate 还会检查图片是否过期、前置分析是否完成、任务是否重复。条件不满足就阻断、延迟或去重。因此模型选择不是最终权限。
2. 当你们的用户量起来之后，你们是怎么做这个防止大模型API限流的处理的, 因为我们也知道大模型的API请求是有并发限制的，比方说你100个用户可以同时请求，但是当你这个用户量来到1000或者1万的时候，你们的模型API肯定是会报429的，这个时候你们怎么处理呢, 有做处理吗？
- 我们是做了压测, 因为这一部分当用户量起来之后，确实是一个问题啊. 我们这里也是做了一个比较通用的方案嘛，就是使用Redis去做一个Redis队列, 我们所有对 Agent 的请求都会先进入队列，然后再从队列取出来进行回复, 然后我们还采取了一个指数退币的这个从事机制嘛，像就是第一次我们会使用第一次出错，一秒后重发；第二次再出错，重发时间变成两秒；第三次变成 4 秒；第四次变成 8 秒。如果超过 8 秒的重试还是不行，我们会返回一个默认的兜底回复：“请稍后重试”。但是基本上都不会出现这种情况。


因为这个也不是我一个人做的，但是我知道怎么回事儿，反正这一套下来，巴拉巴拉巴拉。

3. 关键的几个工具设计:
- readiness gate: 执行工具前会检查前置任务状态, 是否有可以用的自拍, 是否提交重复任务, 图片分析完成了吗?
- check_runtime_status: 对于类似用户的提问, 比如深度报告好了没有, 